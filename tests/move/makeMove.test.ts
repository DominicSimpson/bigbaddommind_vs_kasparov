import { describe, it, expect } from 'vitest';
import { ChessBoard } from '../../src/board/ChessBoard';
import { algebraicToCoords, hasLegalMoveByAlgebraicNotation } from '../move/utils/moveTestUtils';

describe('makeMove', () => {

    it('moves a piece and updates the board state', () => {
        const board = new ChessBoard();
        // Test implementation here
    })

});
